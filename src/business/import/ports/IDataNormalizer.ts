/**
 * İSTEBUL Business Import Engine — normalizer portu.
 *
 * İş sözleşmesi BusinessDataset katmanı ile paylaşılır (Architecture Freeze v1.0).
 * Implementasyon bu engine altında kayıt edilir; bu dosya import yüzeyini tanımlar.
 */

export type { IDataNormalizer } from '../../dataset/normalizers/NormalizerInterfaces';
