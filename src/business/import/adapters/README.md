# adapters

Kaynak tipi adapter **kayıt sistemi** (`AdapterRegistry.ts`).

Henüz `IImportReader` implementasyonu yoktur. Her kayıt `datasetSourceType` ile BusinessDataset `BusinessSource` tipine eşlenir.
