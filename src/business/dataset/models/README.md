# models

BusinessDataset çekirdek modelleri:

- `BusinessDataset` — kök model
- `BusinessMetadata`, `BusinessDatasetVersion`, `BusinessSource`
- `BusinessEntity`, `BusinessColumn`, `BusinessRow`
- `BusinessRelation`, `BusinessAttachment`, `BusinessValidationResult`

Yalnızca interface / type; iş mantığı yoktur.
