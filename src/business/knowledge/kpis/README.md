# kpis

KPI tanımları ve örnek kayıtlar.

- `KPIDefinition.ts` — tip sözleşmesi
- `KPIRegistry.ts` — örnek KPI listesi

Hesaplama motoru bu klasörde yoktur. Yeni KPI için `KPIRegistry.ts`’e tanım ekleyin; raporlarda `kpiIds` ile bağlayın.
