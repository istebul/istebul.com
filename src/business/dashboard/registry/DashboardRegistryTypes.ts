/**
 * Dashboard profil kayıt girişi.
 */

export interface DashboardProfileDefinition {
  id: string;
  name: string;
  description: string;
  reportDnaId: string;
  defaultLayoutId: string;
  defaultThemeId: string;
  version: string;
}
